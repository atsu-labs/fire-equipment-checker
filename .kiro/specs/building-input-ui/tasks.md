# Implementation Plan: building-input-ui

## Task Breakdown

### Phase 1: 基盤UIコンポーネント

- [ ] 1. 汎用UIプリミティブコンポーネントの実装
- [x] 1.1 (P) 汎用入力フィールドコンポーネントの作成
  - テキスト・数値入力に対応した再利用可能な入力コンポーネントを実装
  - ラベル、単位表示、補足説明(ヘルプテキスト)の表示機能を追加
  - エラーメッセージの表示とスタイリング(赤枠、赤文字)を実装
  - React Hook Formの`register` propsを受け取れるインターフェースを定義
  - Tailwind CSSでレスポンシブ対応のスタイリングを適用
  - _Requirements: 2.1, 2.4, 7.1_

- [x] 1.2 (P) 汎用セレクトボックスコンポーネントの作成
  - ドロップダウン選択UIを実装
  - 通常オプションとグループ化オプションの両方に対応
  - エラー表示機能を追加
  - React Hook Formとの統合インターフェースを実装
  - Tailwind CSSでアクセシブルなスタイリングを適用
  - _Requirements: 1.1, 1.2, 1.3, 7.1_

- [x] 1.3 (P) 汎用ボタンコンポーネントの作成
  - primary, secondary, dangerのバリアント対応
  - サイズバリエーション(sm, md, lg)の実装
  - lucide-reactアイコンの統合
  - ローディング状態表示機能を追加
  - 非活性状態のスタイリングを実装
  - _Requirements: 7.3_

- [x] 1.4 (P) チェックボックス・エラーメッセージ・確認ダイアログコンポーネントの作成
  - チェックボックスコンポーネントを実装(ラベル、エラー表示対応)
  - エラーメッセージコンポーネントを実装(AlertCircleアイコン統合)
  - 確認ダイアログコンポーネントを実装(タイトル、メッセージ、確認/キャンセルボタン)
  - Tailwind CSSでモーダルスタイリングを適用
  - キーボード操作(Escapeキー)対応を実装
  - _Requirements: 4.1, 4.2, 5.2, 7.5, 7.6, 8.4_

### Phase 2: Zodスキーマ拡張

- [ ] 2. バリデーションスキーマの拡張
- [x] 2.1 階別用途詳細のZodスキーマ定義
  - `FloorUsageDetail`型に対応するZodスキーマを作成
  - 階数(整数)、用途コード、床面積(正の数、最大100,000)のバリデーションを定義
  - 収容人員(任意、非負整数)のバリデーションを定義
  - 階全体属性(無窓階、避難階、直通階段数等)のバリデーションを定義
  - エラーメッセージを日本語化
  - _Requirements: 3.2, 3.5, 4.1, 4.2, 4.3_

- [x] 2.2 包括的な建築物情報フォームスキーマの作成
  - 基本属性(用途コード、延床面積、階数、地階数)のスキーマを統合
  - 階別用途詳細配列(`z.array(floorUsageDetailSchema)`)を追加
  - 特殊属性(無窓階、避難階、直通階段数等)のバリデーションを追加
  - 複合用途((16)項)の場合の`floorUsageDetails`必須検証をrefineで実装
  - 数値範囲(延床面積最大1,000,000㎡、階数最大200等)のバリデーションを定義
  - 小数第2位までの受け入れ設定を追加
  - 全フィールドのエラーメッセージを日本語化
  - _Requirements: 1.5, 2.2, 2.3, 2.5, 3.1, 4.1, 4.2, 4.3, 4.4, 4.5, 5.1_

### Phase 3: セクションコンポーネント実装

- [ ] 3. 用途コード選択セクションの実装
- [x] 3.1 用途コードのカテゴリ別グループ化ロジックの作成
  - `USAGE_TYPES`マスタデータからカテゴリ別にグループ化するヘルパー関数を実装
  - "(一)項"、"(二)項"等のカテゴリラベルを生成
  - 各用途コードに用途名と法令参照を併記した表示ラベルを生成
  - TypeScript型安全性を維持したデータ構造を返却
  - _Requirements: 1.2, 1.3_

- [x] 3.2 用途コード選択コンポーネントの実装
  - React Hook Formの`control` propsを受け取るインターフェースを実装
  - グループ化セレクトボックスUIを構築(カテゴリ別optgroup使用)
  - 選択変更時のコールバック処理を実装
  - バリデーションエラー表示を統合
  - 複合用途((16)項)選択検出ロジックを実装
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 4.  基本属性入力セクションの実装
- [x] 4.1 基本属性入力セクションコンポーネントの作成
- 延床面積、階数、地階数の3つの数値入力フィールドを配置
- 各フィールドにラベル、単位(㎡、階)、ヘルプテキストを表示
- React Hook Formの`control`と`errors`を統合
- バリデーションエラーをフィールド下部に表示
- レスポンシブレイアウト(グリッドまたはフレックスボックス)を実装
- _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

- [x] 5. 階別用途詳細入力セクションの実装
- [x] 5.1 階別用途詳細テーブルコンポーネントの作成
  - React Hook Formの`useFieldArray`で動的配列を管理
  - テーブルヘッダー(階数、用途コード、床面積、収容人員、属性)を表示
  - 各行に入力フィールド(数値入力、用途セレクト、チェックボックス)を配置
  - 行追加ボタン(Plusアイコン)を実装し、`append()`メソッドを呼び出し
  - 行削除ボタン(Trashアイコン)を実装し、`remove(index)`メソッドを呼び出し
  - 地階入力(負の数)に対応
  - _Requirements: 3.1, 3.2, 3.3, 3.5_

- [x] 5.2 用途別床面積合計の自動計算機能の実装
  - `floorUsageDetails`配列から用途コード別に床面積を集計するロジックを作成
  - `useMemo`でメモ化し、パフォーマンスを最適化
  - 集計結果をテーブル下部に表示(用途コード: 合計面積㎡)
  - 100行のデータでも遅延なく動作することを確認
  - _Requirements: 3.6_

- [x] 5.3 階全体属性の共通設定管理機能の実装
  - 同一階に複数用途がある場合の属性共有ロジックを実装
  - 無窓階、避難階、直通階段数等の設定を階単位で管理
  - UIで階別属性セクションを表示し、属性変更時に同一階の全行を更新
  - _Requirements: 3.4_

- [x] 6. 特殊属性入力セクションの実装
- [x] 6.1 特殊属性入力セクションコンポーネントの作成
  - 無窓階、避難階、避難上有効な屋外階段、防火壁区画のチェックボックスを配置
  - React Hook Formの`watch('floors')`で階数を監視
  - 階数が3階以上の場合のみ直通階段数入力フィールドを表示(条件付きレンダリング)
  - 各フィールドにラベルとヘルプテキストを表示
  - バリデーションエラーを統合
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_

### Phase 4: メインフォーム統合

- [ ] 7. メインフォームコンポーネントの実装
- [x] 7.1 BuildingInputFormコンポーネントの基本構造作成
  - React Hook Formの`useForm`を初期化(Zod Resolver統合)
  - `mode: 'onBlur'`でフォーカスアウト時バリデーションを設定
  - フォーム状態(isLoading, submitError)を管理
  - 初期データ(復元用)を受け取るpropsを定義
  - 送信成功/エラー時のコールバックpropsを定義
  - _Requirements: 5.5, 5.6, 6.1_

- [x] 7.2 セクションコンポーネントの統合と配置
  - 用途コード選択セクションをフォーム上部に配置
  - 基本属性入力セクションを配置
  - 特殊属性入力セクションを配置
  - React Hook Formの`control`と`errors`を各セクションにprops渡し
  - レスポンシブレイアウト(縦並び、適切な間隔)を実装
  - _Requirements: 7.1, 7.4_

- [ ] 7.3 複合用途モード切り替え機能の実装
  - `watch('usageCode')`で用途コード選択を監視
  - 選択値が16系(複合用途)の場合に階別詳細テーブルを表示
  - 条件付きレンダリング(`{usageCode?.startsWith('16') && <FloorUsageTable />}`)を実装
  - モード切り替え時のスムーズな表示/非表示アニメーションを追加(任意)
  - _Requirements: 1.4, 3.1_

- [ ] 7.4 バリデーションとボタン制御の実装
  - `formState.errors`を監視し、エラー存在時に判定実行ボタンを非活性化
  - エラーがない場合にボタンを活性化
  - フィールドごとのエラーメッセージ表示を統合
  - トップレベルエラーサマリーを表示(任意)
  - _Requirements: 5.2, 5.3, 5.4, 7.6_

### Phase 5: localStorage統合

- [ ] 8. フォームデータ永続化機能の実装
- [ ] 8.1 localStorage統合カスタムフックの作成
  - `useLocalStorageForm`カスタムフックを実装
  - localStorage読み込み処理(初期化時)を実装
  - localStorage保存処理(debounce 500ms)を実装
  - localStorage削除処理を実装
  - エラーハンドリング(容量超過、無効なJSON)を実装
  - _Requirements: 8.1, 8.2_

- [ ] 8.2 自動保存機能の統合
  - `useEffect`でフォーム全体の`watch()`を監視
  - 入力変更時にdebounced保存処理を呼び出し
  - ページリロード時にlocalStorageからデータを復元
  - React Hook Formの`reset()`メソッドで復元データをセット
  - _Requirements: 8.1, 8.2_

- [ ] 8.3 入力内容クリア機能の実装
  - "入力内容をクリア"ボタンを配置
  - ボタンクリック時に確認ダイアログを表示("入力内容を削除しますか?")
  - 確認時にReact Hook Formの`reset()`でフォームをリセット
  - localStorageのデータを削除
  - _Requirements: 8.3, 8.4, 8.5_

### Phase 6: API統合

- [ ] 9. 規制判別APIエンドポイントの実装
- [ ] 9.1 API Routeの作成と基本構造実装
  - `app/api/judge/route.ts`ファイルを作成
  - `POST`メソッドハンドラーを実装
  - リクエストボディのJSON解析処理を追加
  - レスポンスのJSON返却処理を実装
  - _Requirements: 6.2_

- [ ] 9.2 APIバリデーションと規制判別エンジン統合
  - Zodスキーマ(`buildingInfoFormSchemaExtended`)でリクエストボディを検証
  - バリデーションエラー時に400レスポンスを返却(詳細エラーメッセージ付き)
  - `RegulationJudge`クラスをインポートし、インスタンス化
  - `judgeRequiredEquipment()`メソッドを呼び出し、判定結果を取得
  - 成功時に200レスポンスでJudgmentResultを返却
  - ビジネスロジックエラー時に422レスポンスを返却
  - サーバーエラー時に500レスポンスを返却
  - _Requirements: 5.1, 6.2_

- [ ] 10. フォーム送信機能の実装
- [ ] 10.1 フォーム送信ハンドラーの実装
  - `handleSubmit`でフォーム送信イベントをハンドリング
  - 送信中状態(isLoading)を設定し、ボタンにローディングインジケーターを表示
  - `/api/judge`エンドポイントにPOSTリクエストを送信(fetch API使用)
  - リクエストボディに`BuildingInfo`型データを含める
  - _Requirements: 6.1, 6.4_

- [ ] 10.2 API応答処理とエラーハンドリング
  - 成功レスポンス(200)を受信時に結果データをパース
  - localStorageの保存データをクリア
  - 結果画面への遷移処理を実装(Next.js Router使用)
  - ネットワークエラー時に"通信エラーが発生しました"エラーメッセージを表示
  - APIエラー(400, 422, 500)時にエラーメッセージを表示
  - ローディング状態を解除
  - _Requirements: 6.3, 6.5_

### Phase 7: ページ統合

- [x] 11. トップページの置き換え
- [x] 11.1 app/page.tsxの書き換え
  - Next.jsデフォルトプレースホルダーを削除
  - `BuildingInputForm`コンポーネントをインポートして配置
  - ページタイトルとヘッダーを追加("消防用設備規制判別システム - 建築物情報入力")
  - Tailwind CSSでページレイアウトを実装(中央寄せ、最大幅設定)
  - レスポンシブ対応(タブレット・デスクトップ)
  - _Requirements: 7.1, 7.4_

### Phase 8: テスト実装

- [ ] 12. 単体テストの作成
- [ ]\* 12.1 UIプリミティブコンポーネントのテスト
  - `ui/Input`, `ui/Select`, `ui/Button`のレンダリングテストを作成
  - エラー表示、単位表示、アイコン表示のテストを実装
  - ユーザー操作(クリック、入力)のシミュレーションテストを追加
  - _Requirements: 1.1, 1.2, 2.1, 2.2, 7.3_

- [ ]\* 12.2 セクションコンポーネントのテスト
  - `UsageCodeSelect`のカテゴリ別グループ化テストを作成
  - `BasicAttributesSection`のバリデーションエラー表示テストを作成
  - `FloorUsageTable`の行追加/削除テストを作成
  - 用途別床面積合計の自動計算ロジックテストを作成
  - _Requirements: 1.3, 2.2, 3.3, 3.6_

- [ ]\* 12.3 BuildingInputFormの統合テスト
  - localStorage自動保存・復元のテストを作成
  - 複合用途モード切り替えのテストを作成
  - バリデーションエラー表示とボタン制御のテストを作成
  - フォーム送信処理のモックテストを作成
  - _Requirements: 1.4, 5.2, 5.3, 5.4, 8.1, 8.2_

- [ ] 13. E2Eテストの作成
- [ ] 13.1 単一用途建築物入力フローのE2Eテスト
  - Playwrightテストファイルを作成
  - ページアクセス、用途コード選択、基本属性入力のテストステップを実装
  - 判定実行ボタンクリック、API呼び出し検証を実装
  - 結果画面遷移の検証を実装
  - _Requirements: 1.1, 2.1, 6.2, 6.5_

- [ ] 13.2 複合用途建築物入力フローのE2Eテスト
  - 複合用途((16)項)選択のテストを実装
  - 階別詳細テーブルでの行追加・入力のテストを実装
  - 用途別床面積合計表示の検証を実装
  - 判定実行から結果画面遷移までのフローテストを実装
  - _Requirements: 1.4, 3.1, 3.2, 3.3, 3.6, 6.5_

- [ ] 13.3 エラーハンドリングとデータ永続化のE2Eテスト
  - バリデーションエラー表示から修正、送信成功までのテストを実装
  - localStorage自動保存→ページリロード→データ復元のテストを実装
  - 入力内容クリア(確認ダイアログ経由)のテストを実装
  - ネットワークエラーシミュレーションとエラーメッセージ表示のテストを実装
  - _Requirements: 5.2, 6.3, 8.1, 8.2, 8.3, 8.4_

---

_generated_at: 2025-11-09_
